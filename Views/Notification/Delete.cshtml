@model AdminDashboard.Models.Notification

<h2>Xác nhận xóa thông báo</h2>

<p>Bạn có chắc chắn muốn xóa thông báo "<strong>@Model.Title</strong>" không?</p>

<form method="post">
    <button type="submit" class="btn btn-danger">Xóa</button>
    <a href="@Url.Action("Index", "Notification")" class="btn btn-secondary">Hủy</a>
</form>
